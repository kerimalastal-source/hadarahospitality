---
import { LOCALES, LOCALE_LABELS, localizePath, type Locale } from '../i18n';

interface Props {
  locale: Locale;
  /** Site-root, locale-agnostic path for the current page, e.g. "/about". */
  path: string;
}

const { locale, path } = Astro.props;

// 404.astro is the one intentionally-unlocalized page on the site (see
// CLAUDE.md), so it has no /ar, /fr or /ru equivalent to link to — send
// the switcher to that locale's homepage instead of a dead link.
const targetPath = path === '/404' ? '/' : path;
---
<div class="lang-switcher">
  <button type="button" class="lang-switcher-toggle" aria-haspopup="true" aria-expanded="false" data-lang-toggle>
    {LOCALE_LABELS[locale]}
    <svg viewBox="0 0 24 24" width="12" height="12" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9" /></svg>
  </button>
  <ul class="lang-switcher-menu" data-lang-menu>
    {LOCALES.map((l) => (
      <li>
        <a href={localizePath(l, targetPath)} aria-current={l === locale ? 'true' : undefined}>{LOCALE_LABELS[l]}</a>
      </li>
    ))}
  </ul>
</div>
